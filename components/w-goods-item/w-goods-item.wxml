<!--components/w-goods-item/w-goods-item.wxml-->
<view class="goods-item" bindtap="pushDetailPage" data-iid='{{item.iid}}'>
  <image src="{{item.image || item.img || item.show.img}}"  class="image" />
  <view class="desc-info">
    <view class="title">{{item.title}}</view>
    <view class="info">
      <text class="price">￥{{item.price}}</text>
      <image class="icon" src="/assets/common/collect.png" />
      <text class="cfav">{{item.cfav}}</text>
    </view>
  </view>
</view>
